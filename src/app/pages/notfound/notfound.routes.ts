import { Routes } from '@angular/router';
import { NotFoundComponent } from './notfound.component';

export const NOTFOUND_COMPONENT_ROUTES: Routes = [
  {
    path: '',
    component: NotFoundComponent,
  }
];
