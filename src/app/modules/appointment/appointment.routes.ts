import { Routes } from '@angular/router';
import { AppointmentComponent } from './appointment.component';

export const APPOINTMENT_COMPONENT_ROUTES: Routes = [
  {
    path: '',
    component: AppointmentComponent
  }
];
